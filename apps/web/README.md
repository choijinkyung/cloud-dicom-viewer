# apps/web

Next.js 기반 워크리스트 / viewer shell / admin UI 앱입니다.

현재 포함:

- `app/page.tsx` 제품 랜딩
- `app/worklist/page.tsx` 워크리스트 프리뷰
- `app/studies/[studyInstanceUid]/page.tsx` viewer shell

다음 단계:

- Keycloak 로그인 연동
- API 기반 worklist fetch
- OHIF / Cornerstone 연동
- 관리자 화면 추가
